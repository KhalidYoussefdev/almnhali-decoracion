import { SEED_CATEGORIES, SEED_COLLECTIONS } from '@/data/seed';

/** @deprecated Use settings.categories via getSettings() or useSettings() */
export const categories = SEED_CATEGORIES;

/** @deprecated Use settings.collections via getSettings() or useSettings() */
export const collections = SEED_COLLECTIONS;